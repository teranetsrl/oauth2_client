name: oauth2_client_example
description: Example for the OAuth2 client library
version: 1.0.0
homepage: https://github.com/ThexXTURBOXx/oauth2_client
repository: https://github.com/ThexXTURBOXx/oauth2_client
issue_tracker: https://github.com/ThexXTURBOXx/oauth2_client/issues

publish_to: none

environment:
  sdk: '>=3.5.0 <3.11.0'

dependencies:
  flutter:
    sdk: flutter
  oauth2_client: # Normally you would need to specify something like "^4.0.0" here
    path: ..

dev_dependencies:
  flutter_lints: ^5.0.0
